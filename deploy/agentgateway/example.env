# Provider credentials used only by the host-native agentgateway process.
# Copy this file to .agentgateway.env at the repository root, keep mode 0600,
# use shell assignment syntax, and reference variables from config.yaml:
#
# DEEPSEEK_API_KEY='replace-locally'
# config.yaml: apiKey: "$DEEPSEEK_API_KEY"
